[project]
name = "bms-alarm-timeseries-periodic"
version = "1.0.0"
description = "A set of scripts associated with the dataset 'BMS alarm timeseries with periodicity labels'."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib-venn>=1.1.2",
    "notebook>=7.5.6",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
]
